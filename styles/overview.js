
// !Packages
import { StyleSheet } from 'react-native'

export const overview = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#fff',
        alignItems: 'center',
        justifyContent: 'center',
    },
})